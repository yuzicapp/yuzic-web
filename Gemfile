source "https://rubygems.org"
gem 'github-pages'
# Ruby 3.4+ removed these from the default gems; Jekyll 3.9 still requires
# them. GitHub Pages builds with its own bundle, so this is for local builds.
gem "csv"
gem "base64"
gem "logger"
gem "bigdecimal"
